/** RougeChain testnet API (see https://docs.rougechain.io) */
export const ROUGECHAIN_API = 'https://testnet.rougechain.io/api';

/** WebSocket endpoint for real-time tx/block events */
export const ROUGECHAIN_WS = 'wss://testnet.rougechain.io/api/ws';

/** Qwalla's own mail domain — resolves the same on-chain registry as rouge.quant */
export const MAIL_DOMAIN = 'qwalla.mail';

export const TRANSFER_FEE = 0.1;
