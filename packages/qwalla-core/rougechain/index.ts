/**
 * rougechain — RougeChain client, websocket, fees, and API wrappers over
 * @rougechain/sdk (pure JS, runs everywhere).
 *
 * Pending migration from lib/: rougechain, ws, fees, evm-rpc, messenger-api,
 * mail-api, names.
 */
export {};
